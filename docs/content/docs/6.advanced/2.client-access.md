---
title: Client Access
description: Access the raw Convex client instance for advanced use cases.
navigation:
  icon: i-lucide-box
---

## Basic Usage

```vue
<script setup lang="ts">
import { api } from '#convex/api'

const convex = useConvex()

onMounted(async () => {
  // Direct client access
  const result = await convex.query(api.tasks.list)
  console.log(result)
})
</script>
```

## Return Value

| Type           | Description                 |
| -------------- | --------------------------- |
| `ConvexClient` | The Convex client instance. |

## Important Notes

**Client-only**: The `ConvexClient` uses WebSockets and only works in the browser. During SSR, `useConvex()` throws an error.

```ts
const convex = useConvex()
// Throws if called when client is unavailable
```

## When to Use

In most cases, prefer the higher-level composables:

| Use Case        | Recommended Composable    |
| --------------- | ------------------------- |
| Fetching data   | `useConvexQuery`          |
| Modifying data  | `useConvexMutation`       |
| Running actions | `useConvexAction`         |
| Paginated data  | `useConvexPaginatedQuery` |
| Auth state      | `useConvexAuth`           |

Use `useConvex()` when you need:

- Direct access to the client for advanced patterns
- Integration with external libraries expecting a Convex client
- Custom subscription management
- Access to client methods not exposed by composables

## Example: Custom Subscription

```vue
<script setup lang="ts">
import { api } from '#convex/api'

const convex = useConvex()
const notifications = ref<Notification[]>([])

onMounted(() => {
  // Manual subscription with custom handling
  const unsubscribe = convex.onUpdate(api.notifications.list, { userId: 'user123' }, (result) => {
    notifications.value = result
    // Custom logic: play sound, show toast, etc.
    if (result.some((n) => n.unread)) {
      playNotificationSound()
    }
  })

  onUnmounted(() => {
    unsubscribe()
  })
})
</script>
```

## One-Shot Calls (`useConvexCall`)

For imperative one-shot calls from client middleware, plugins, or effects, `useConvexCall()` returns `query`/`mutation`/`action` (throwing) and `querySafe`/`mutationSafe`/`actionSafe` (returning a `CallResult` that never throws).

```ts
const { mutation, querySafe } = useConvexCall()

await mutation(api.tasks.complete, { id })

const result = await querySafe(api.tasks.list)
if (result.ok) {
  // result.value
}
```

`useConvexCall()` takes no options.

::callout{icon="i-lucide-alert-triangle"}
**No timeout by design.** A client-side timeout cannot cancel an in-flight mutation or action — a "timed out" call can still commit on the server, so presenting it as failed-and-safely-retriable invites duplicate writes. If you need a deadline, race the returned promise yourself and treat a rejection as "may still have committed" (re-check state before retrying). Unauthorized failures are routed through the same opt-in recovery pipeline as `useConvexMutation`.
::

## Related Topics

- [Fetching Data](/docs/data-fetching/queries) - SSR-compatible queries with real-time updates
- [Mutations](/docs/mutations/mutations) - Mutations with optimistic updates
- [Convex Client Documentation](https://docs.convex.dev/client/javascript) - Official Convex client docs
