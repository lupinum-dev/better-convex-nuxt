---
title: Performance
description: Optimize authentication and SSR performance with static JWKS and caching strategies.
navigation:
  icon: i-lucide-gauge
---

## Quick Wins

1. **Use `subscribe: false`** for data that doesn't need real-time updates (config, reports, search results). This eliminates WebSocket subscription overhead.
2. **Use `auth: 'none'`** on public-page queries that never need identity. Skips waiting on auth settlement and always executes anonymously.

---

## Static JWKS

By default, the Convex Better Auth plugin keeps its signing key in the Better Auth JWKS table and exposes a dynamic JWKS endpoint to Convex. Signing and key retrieval therefore use the component database.

Static JWKS removes those runtime key lookups by storing the current key document in an environment variable. It also turns key synchronization into an operator-owned release ceremony; do not enable it only as a speculative optimization.

### When to use

- High-traffic authenticated SSR pages
- When you notice slow initial page loads
- Reducing Convex function calls/bandwidth

### Setup

**1. Add the JWKS generation action to your Convex backend:**

```ts [convex/auth.ts]
import { internalAction } from './_generated/server'

export const getLatestJwks = internalAction({
  args: {},
  handler: async (ctx) => {
    const auth = createAuth(ctx)
    return await auth.api.getLatestJwks()
  },
})
```

**2. Generate and set the JWKS environment variable:**

```bash
npx convex run auth:getLatestJwks | npx convex env set JWKS
```

**3. Update your auth config to use static JWKS:**

```ts [convex/auth.config.ts]
import { getAuthConfigProvider } from '@convex-dev/better-auth/auth-config'
import type { AuthConfig } from 'convex/server'

const jwks = process.env.JWKS

export default {
  providers: [getAuthConfigProvider({ jwks })],
} satisfies AuthConfig
```

**4. Update the Better Auth plugin:**

```ts [convex/auth.ts]
const jwks = process.env.JWKS

export const createAuth = (ctx: GenericCtx<DataModel>) => {
  return betterAuth({
    // ... other config
    plugins: [convex({ authConfig, jwks })],
  })
}
```

### Trade-offs

| Aspect       | Dynamic JWKS (default)                         | Static JWKS                                                                  |
| ------------ | ---------------------------------------------- | ---------------------------------------------------------------------------- |
| Setup        | Automatic                                      | Requires CLI command                                                         |
| Performance  | 2-4 DB calls per token op                      | 0 DB calls                                                                   |
| Key creation | Automatic when absent                          | Generate before snapshot                                                     |
| Key rotation | No graceful routine ceremony in adapter 0.12.5 | No graceful routine ceremony; coordinated environment/deploy update required |
| Key storage  | Database                                       | Environment variable                                                         |

### Rotation limitation

::callout{icon="i-lucide-shield-alert"}
`@convex-dev/better-auth` 0.12.5 does not provide a verified zero-downtime rotation/overlap/rollback ceremony. Its server-only `rotateKeys()` action deletes the existing JWKS rows before generating a replacement. Treat that as emergency invalidation: previously issued JWTs stop verifying after Convex's cached JWKS refreshes, and static deployments can temporarily disagree about the active key. `getLatestJwks()` only snapshots the current key; it does not rotate it.
::

Do not claim routine automatic rotation or use an untested production command sequence. Before rotating, rehearse the exact dynamic/static topology in staging, record cache behavior, coordinate the signing key and Convex auth configuration update, expect active reauthentication, keep a rollback plan, and verify both a newly issued token and intentional rejection of the retired key. Reassess this guidance when the pinned adapter exposes a supported overlap ceremony.

### When to refresh the static snapshot

- Immediately as part of an explicitly rehearsed emergency rotation
- If you see JWT verification errors
- After a fresh deployment without existing keys

```bash
npx convex run auth:getLatestJwks | npx convex env set JWKS
```

---

## Disable Subscriptions

For data that doesn't need real-time updates, use `subscribe: false` to switch to strict HTTP-only fetching (no WebSocket subscription dependency):

```ts
const { data, refresh } = await useConvexQuery(api.config.getSettings, {}, { subscribe: false })
```

### Benefits

- Reduces WebSocket connections
- Fewer Convex function calls
- Lower bandwidth usage

### When to use

- Configuration/settings that rarely change
- Historical data or reports
- Search results (use `refresh()` to re-fetch)
- Static content

### Manual refresh

With `subscribe: false`, data won't update automatically. Use `refresh()` when needed (HTTP re-fetch):

```ts
const { data, refresh } = await useConvexQuery(
  api.posts.search,
  computed(() => (query.value ? { q: query.value } : 'skip')),
  { subscribe: false },
)

// Trigger search manually
async function doSearch() {
  await refresh()
}
```

---

## Token Caching

The module automatically caches tokens on the client to avoid redundant validation requests. When Convex's client requests a token refresh, we return the cached token if it was validated recently (within 10 seconds).

This is handled automatically - no configuration needed.

---

## Query Deduplication

Multiple components calling the same query share a single subscription. The module keeps a per-Nuxt-app subscription cache keyed by query name and normalized arguments.

```vue
<!-- Both components share the same subscription -->
<template>
  <Header />
  <!-- calls useConvexQuery(api.user.current, {}) -->
  <Sidebar />
  <!-- also calls useConvexQuery(api.user.current, {}) -->
</template>
```

No duplicate fetches, no duplicate subscriptions.

---

## Understanding Duplicate Query Calls

In your Convex dashboard, you may see queries called twice:

```
Q auth:getCurrentUser (HTTP API)     <- SSR
Q teams:listMyTeams   (HTTP API)     <- SSR
Q auth:getCurrentUser (WebSocket)    <- Client subscription
Q teams:listMyTeams   (WebSocket)    <- Client subscription
```

**This is expected behavior.** Here's why:

| Call      | Purpose                  | When             |
| --------- | ------------------------ | ---------------- |
| HTTP API  | SSR - embed data in HTML | Server render    |
| WebSocket | Real-time subscription   | Client hydration |

### Why both are needed

1. **HTTP (SSR)**: One-time fetch to include data in the initial HTML. Fast first paint, SEO-friendly.

2. **WebSocket (Subscription)**: Ongoing connection for real-time updates. If another user changes data, your UI updates automatically.

### Are they expensive?

No. Notice they show `(cached)` and `0ms` duration:

```
success (cached) Q auth:getCurrentUser  <- 0ms, no re-computation
```

Convex is smart - the WebSocket subscription returns cached data instantly since the query result hasn't changed.

### When to optimize

**Option 1: `subscribe: false`** - SSR only, no WebSocket subscription:

```ts
const { data } = await useConvexQuery(api.config.get, {}, { subscribe: false })
```

Dashboard shows:

```
Q config:get (HTTP API)  <- SSR only
```

**Option 2: `server: false`** - Client only, no SSR:

```ts
const { data } = await useConvexQuery(api.session.get, {}, { server: false })
```

Dashboard shows:

```
Q session:get (WebSocket)  <- Client only
```

**Option 3: Both** - No SSR, no subscription (one-time client fetch):

```ts
const { data, refresh } = await useConvexQuery(
  api.reports.get,
  {},
  { server: false, subscribe: false },
)
```

Dashboard shows:

```
Q reports:get (HTTP API)  <- Single client fetch via HTTP
```

---

## Measuring Performance

Use browser DevTools Network tab to measure:

- **TTFB (Time to First Byte)**: Should improve with static JWKS
- **Token endpoint time**: `/api/auth/convex/token` response time
- **Convex dashboard**: Check function call counts

Before static JWKS, you'll see `adapter:findMany` calls for JWKS. After, these disappear.

---

## Related Topics

- [Fetching Data](/docs/data-fetching/queries) - Query composable with `subscribe` option
- [SSR & Hydration](/docs/server-side/ssr-hydration) - Server-side rendering patterns
- [Authentication](/docs/auth-security/authentication) - Authentication composable
