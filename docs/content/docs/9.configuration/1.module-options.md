---
title: 'Module Options'
description: 'Complete reference for all nuxt.config convex options.'
---

## Overview

Configure @lupinum/trellis via the `trellis` key in your `nuxt.config.ts`:

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  modules: ['@lupinum/trellis'],
  trellis: {
    // options
  },
})
```

## Core Options

::field-group
::field{name="url" type="string"}
Convex deployment WebSocket URL (e.g., `https://your-app.convex.cloud`). Auto-detected from `CONVEX_URL` or `NUXT_PUBLIC_CONVEX_URL` environment variables.
::
::

## Authentication

::field-group
::field{name="auth" type="boolean | AuthOptions"}
Enable authentication. Default: `true`. Pass `true` for defaults or an object for detailed configuration.
::
::field{name="auth.route" type="string"}
Auth proxy route path. Default: `'/api/auth'`.
::
::field{name="auth.trustedOrigins" type="string[]"}
CORS trusted origins for the auth proxy. Useful for preview deployments (e.g., `['https://*.vercel.app']`).
::
::field{name="auth.skipAuthRoutes" type="string[]"}
Glob patterns for routes that skip auth token fetch.
::
::

### Route Protection

::field-group
::field{name="auth.routeProtection.redirectTo" type="string"}
Default redirect path for protected pages. Default: `'/auth/signin'`.
::
::field{name="auth.routeProtection.preserveReturnTo" type="boolean"}
Add `?redirect=/original-path` to the redirect URL. Default: `true`.
::
::

### Unauthorized Handling

Automatically redirects to a sign-in page when Convex returns 401/403 and the user is **not currently authenticated**. If the user is authenticated, the error is treated as a business-logic permission denial (e.g., "not an admin") and the redirect is skipped.

::field-group
::field{name="auth.unauthorized.enabled" type="boolean"}
Automatically handle 401/403 errors from Convex. Default: `false`.
::
::field{name="auth.unauthorized.redirectTo" type="string"}
Redirect path when an unauthorized error is detected.
::
::field{name="auth.unauthorized.includeQueries" type="boolean"}
Also handle auth errors from queries (not just mutations/actions). Default: `false`.
::
::

### Token Caching

::field-group
::field{name="auth.cache.enabled" type="boolean"}
Enable server-side JWT token caching. Default: `false`.
::
::field{name="auth.cache.ttl" type="number"}
Cache time-to-live in seconds (1–60). Default: `60`.
::
::

### Proxy Limits

::field-group
::field{name="auth.proxy.maxRequestBodyBytes" type="number"}
Maximum request body size for auth proxy. Default: `1048576` (1 MB).
::
::field{name="auth.proxy.maxResponseBodyBytes" type="number"}
Maximum response body size from auth proxy. Default: `1048576` (1 MB).
::
::

## Query Defaults

::field-group
::field{name="query.server" type="boolean"}
Run queries server-side during SSR. Default: `true`.
::
::field{name="query.subscribe" type="boolean"}
Keep WebSocket subscriptions active. Default: `true`.
::
::

## Upload Defaults

::field-group
::field{name="upload.maxConcurrent" type="number"}
Maximum concurrent uploads in queue mode. Default: `3`.
::
::

::field-group
::field{name="observability.enabled" type="boolean"}
Enable semantic Trellis observability events. Default: `true`.
::

::field{name="observability.level" type="'critical' | 'normal' | 'verbose'"}
Control semantic event volume. Defaults to `'verbose'` in development and `'critical'` in production. Critical events include denials, destructive flows, `crossTenant`, `raw`, and tool failures.
::

::field{name="observability.capture" type="{ backend?: boolean; mcp?: boolean; browser?: boolean }"}
Enable or disable event capture per transport family. Defaults to backend/MCP on and browser on only in development.
::

::field{name="observability.sample" type="Partial<Record<'identity' | 'authorization' | 'trust' | 'operations' | 'mcp' | 'browser', number>>"}
Optional sample rates per event family. Values must be finite numbers in the range `0..1`.
::

::field{name="observability.service" type="string"}
Service name used consistently for `evlog` delivery across Nuxt, MCP, and Convex emissions.
::

::field{name="observability.correlation.header" type="string"}
Header name used for incoming correlation propagation. Default: `'x-trellis-correlation-id'`.
::
::
