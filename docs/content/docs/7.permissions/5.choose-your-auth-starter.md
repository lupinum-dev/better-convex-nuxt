---
title: 'Choose Your Auth Starter'
description: 'Pick the right starter scaffold, then layer additive auth blocks on top.'
---

## Starter Matrix

| App shape                                              | Command                                                                          | What it scaffolds                                                                                                                             | Read next                                                                        |
| ------------------------------------------------------ | -------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| Authenticated app without tenants                      | `npx better-convex-nuxt init auth` then `init permissions --model personal`      | `defineAuth(...)`, `convex/functions.ts`, `appQuery` / `appMutation`, personal ownership helpers, `convex/test.setup.ts`                      | [`examples/02-auth-todo`](../../../../examples/02-auth-todo/README.md)           |
| Tenant-scoped app                                      | `npx better-convex-nuxt init auth` then `init permissions --model workspace`     | `defineAuth(...)`, `convex/functions.ts`, `createDefaultGetActor()`, `ensureTenant()`, `loadTenantResource()`, `_can`, `convex/test.setup.ts` | [`examples/03-team-workspace`](../../../../examples/03-team-workspace/README.md) |
| Tenant-scoped app with MCP or server-to-server callers | `npx better-convex-nuxt init auth` then `init permissions --model workspace-mcp` | Workspace starter plus `convex/functions.ts`, `createDefaultGetActor()`, trusted-caller transport, and `appQuery` / `appMutation`             | [`examples/07-mcp-reference`](../../../../examples/07-mcp-reference/README.md)   |

Run `npx better-convex-nuxt init mcp` when you also need MCP middleware scaffolding.

## Additive Blocks

Start with one of the starters above, then add vertical helpers as needed:

- Visibility, prerequisite, freemium, and collaboration helpers now live in the examples and recipes docs instead of additive CLI blocks.

The block gallery lives in [`/docs/permissions/saas-examples`](/docs/permissions/saas-examples).

## What The Starter Owns

Every starter copies plain app code into your repo. You keep and modify:

- `convex/auth.ts`
  - `convex/auth.config.ts`
  - `convex/http.ts`
  - `convex/convex.config.ts`
- `convex/functions.ts`
- `convex/auth/*`
- `convex/test.setup.ts`
- `composables/usePermissions.ts`

Nothing here is a hidden runtime builder. The scaffold is only a starting point.

For the workspace boundary files themselves, use the examples as the canonical references:

- single-workspace template: [`examples/03-team-workspace`](../../../../examples/03-team-workspace/README.md)
- memberships template: [`examples/06-multi-workspace`](../../../../examples/06-multi-workspace/README.md)

The canonical starter contract is:

- `workspaceId` on scoped tables
- `by_workspace` indexes
- `users.authId`, `users.role`, and `users.workspaceId`
- `ownerId` storing the auth-subject string

If your app follows that shape, `createTestContext({ schema, modules })` works without extra config.
Reach for the advanced testing overrides only when your schema intentionally diverges.

## Upgrade Paths

- Personal app growing into tenants: [`/docs/permissions/personal-to-workspace`](/docs/permissions/personal-to-workspace)
- Tenant app adding MCP/trusted callers: [`/docs/permissions/workspace-to-workspace-mcp`](/docs/permissions/workspace-to-workspace-mcp)
- Advanced override seams: [`/docs/permissions/overrides-and-advanced-patterns`](/docs/permissions/overrides-and-advanced-patterns)
