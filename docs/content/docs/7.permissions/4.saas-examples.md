---
title: 'SaaS Examples'
description: 'Map each authorization recipe to a runnable example app.'
---

## Overview

The authorization recipes are easier to evaluate when they are attached to a runnable app instead
of a detached snippet.

The examples folder now covers the main SaaS authorization shapes end to end.

## Gallery

| Example                | What it models | What it teaches | Link |
| ---------------------- | ------------- | --------------- | ---- |
| `04-saas-platform`     | A project management tool with teams | Roles, ownership checks, plan-based limits | [Open](https://github.com/lupinum-dev/trellis/blob/main/examples/04-saas-platform/README.md) |
| `05-visibility-access` | A knowledge base where some content is restricted | Visibility filters, data redaction | [Open](https://github.com/lupinum-dev/trellis/blob/main/examples/05-visibility-access/README.md) |
| `06-multi-workspace`   | An agency tool where users belong to multiple clients | Cross-tenant memberships, workspace switching | [Open](https://github.com/lupinum-dev/trellis/blob/main/examples/06-multi-workspace/README.md) |
| `07-mcp-reference`     | A workspace app with full MCP integration | MCP sessions, tools, prompts, resources | [Open](https://github.com/lupinum-dev/trellis/blob/main/examples/07-mcp-reference/README.md) |
| `08-component-mini-cms`| A CMS built as a Convex component | Component bridges, MCP projection, preview | [Open](https://github.com/lupinum-dev/trellis/blob/main/examples/08-component-mini-cms/README.md) |

::tip
**New to these examples?** Start with `03-team-workspace` (the simplest protected app), then jump to whichever example matches your app's shape.
::

## How To Use Them

Read them in either order:

- progression-first: `01` -> `04`
- auth-shape-first: jump straight to the SaaS type that matches your app

If you are still choosing a starter, begin with
[`/docs/permissions/choose-your-auth-starter`](/docs/permissions/choose-your-auth-starter) and then
layer one of these blocks on top.

If you are comparing the examples because of shared validators or `shared/` folders, pair this page with:

- [`/docs/guide/shared-schema-dx`](/docs/guide/shared-schema-dx)
- [`/docs/mcp-tools/shared-schema`](/docs/mcp-tools/shared-schema)

Each example keeps the same ground rules:

- auth rules live in app-owned `convex/auth/*`
- protected handlers are authored through `app.query(...)` / `app.mutation(...)`
- the frontend only reflects backend decisions
- tests prove the hard authorization case for that SaaS type

Keep the progression intentional:

- `03` to `05` use the canonical single-workspace model (`users.workspaceId` + `users.role`)
- `06` shows the memberships-based upgrade when users can belong to multiple workspaces
- `07` adds the broader MCP surface on top
- `08` shows the cleanest local component + bridge + projection seam

See [Supporting Multiple Caller Types](/docs/permissions/actor-lanes-and-models) for how different transports converge on one actor model.
