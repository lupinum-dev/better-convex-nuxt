---
title: User Augmentation
description: Add custom claims typing to useAuth().user with module augmentation.
navigation:
  icon: i-lucide-user-round-cog
---

## Goal

Make `useAuth().user` understand app-specific claims like `role` or `stripeCustomerId`.

## Add Module Augmentation

```ts [types/convex-user.d.ts]
import 'better-convex-nuxt'

declare module 'better-convex-nuxt' {
  interface ConvexUser {
    role?: 'admin' | 'member' | 'viewer'
    stripeCustomerId?: string
    organizationId?: string
  }
}
```

## Use It in Components

```vue
<script setup lang="ts">
const { user } = useAuth()

const isAdmin = computed(() => user.value?.role === 'admin')
</script>

<template>
  <div>
    <p>Customer: {{ user?.stripeCustomerId || 'none' }}</p>
    <p v-if="isAdmin">Admin tools enabled</p>
  </div>
</template>
```

## Best Practice

- Treat `useAuth().user` as UI-friendly identity + claims.
- Keep authoritative authorization in Convex queries/mutations (for example via `usePermissions()` and backend checks).

