---
title: Server Routes
description: Execute Convex operations from Nuxt server routes, middleware, and webhooks.
navigation:
  icon: i-lucide-server
---

These utilities are auto-imported in your `server/` directory.

::callout{icon="i-lucide-info"}
**Important naming distinction:** This page covers **Nitro server code** (`server/api/*`, `server/middleware/*`, webhooks, jobs). It does **not** cover Nuxt app route middleware (`defineNuxtRouteMiddleware`).
::

If you need Convex data inside `defineNuxtRouteMiddleware()` (for auth/permissions during navigation), use one-shot APIs:

- client navigation: `useConvex()` to get the client, then call `.query()`
- SSR navigation: `serverConvexQuery`

`useConvexQuery` and `useConvexPaginatedQuery` are setup-scope composables and should not be used inside route middleware/plugins.

## Available Functions

| Function               | Purpose            |
| ---------------------- | ------------------ |
| `serverConvexQuery`    | Execute a query    |
| `serverConvexMutation` | Execute a mutation |
| `serverConvexAction`   | Execute an action  |

::callout{icon="i-lucide-shield" to="/docs/server-side/server-call-lanes"}
`serverConvex*` is a **user-scoped** server lane. It uses the current Better Auth session when
available, or runs unauthenticated/public when you choose `auth: 'none'`. It is not a backend
superuser channel.
::

---

## serverConvexQuery

Execute a one-off query from server-side code.

```ts [server/api/posts.get.ts]
import { api } from '~~/convex/_generated/api'

export default defineEventHandler(async (event) => {
  const posts = await serverConvexQuery(event, api.posts.list, { status: 'published' })

  return posts
})
```

### API Reference

```ts
function serverConvexQuery<Query>(
  event: H3Event,
  query: Query,
  args?: FunctionArgs<Query>,
  options?: ServerConvexOptions,
): Promise<FunctionReturnType<Query>>
```

| Parameter | Type                  | Description                                  |
| --------- | --------------------- | -------------------------------------------- |
| `event`   | `H3Event`             | Current Nitro event (used for config + auth) |
| `query`   | `FunctionReference`   | Query function reference                     |
| `args`    | `object`              | Query arguments                              |
| `options` | `ServerConvexOptions` | Optional auth policy/token override          |

---

## serverConvexMutation

Execute a mutation from server-side code.

```ts [server/api/tasks/complete.post.ts]
import { api } from '~~/convex/_generated/api'

export default defineEventHandler(async (event) => {
  const body = await readBody(event)

  await serverConvexMutation(event, api.tasks.complete, { taskId: body.taskId })

  return { success: true }
})
```

### API Reference

```ts
function serverConvexMutation<Mutation>(
  event: H3Event,
  mutation: Mutation,
  args?: FunctionArgs<Mutation>,
  options?: ServerConvexOptions,
): Promise<FunctionReturnType<Mutation>>
```

---

## serverConvexAction

Execute an action from server-side code.

```ts [server/api/reports/generate.post.ts]
import { api } from '~~/convex/_generated/api'

export default defineEventHandler(async (event) => {
  const body = await readBody(event)

  const report = await serverConvexAction(event, api.reports.generate, {
    month: body.month,
    year: body.year,
  })

  return report
})
```

### API Reference

```ts
function serverConvexAction<Action>(
  event: H3Event,
  action: Action,
  args?: FunctionArgs<Action>,
  options?: ServerConvexOptions,
): Promise<FunctionReturnType<Action>>
```

---

## ServerConvexOptions

Options available for all fetch functions:

```ts
interface ServerConvexOptions {
  auth?: 'auto' | 'required' | 'none'
  authToken?: string
}
```

::callout{icon="i-lucide-info" to="/docs/advanced/logging"}
Enable module-level logging via the `logging` config option for debugging server-side operations.
::

::callout{icon="i-lucide-alert-triangle"}
If your route should run independently of end-user identity, do not force that through
`serverConvex*`. Use an app-local private bridge instead. See
[Server Call Lanes](/docs/server-side/server-call-lanes) and
[System Workloads: Private Bridge](/docs/recipes/system-workloads-private-bridge).
::

---

## Patterns

### Webhook Handler

```ts [server/api/webhooks/stripe.post.ts]
import { api } from '~~/convex/_generated/api'
import Stripe from 'stripe'

const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!)

export default defineEventHandler(async (event) => {
  const body = await readRawBody(event)
  const signature = getHeader(event, 'stripe-signature')!

  // Verify webhook
  const stripeEvent = stripe.webhooks.constructEvent(
    body!,
    signature,
    process.env.STRIPE_WEBHOOK_SECRET!,
  )

  // Handle the event
  switch (stripeEvent.type) {
    case 'checkout.session.completed': {
      const session = stripeEvent.data.object
      await serverConvexMutation(event, api.subscriptions.activate, {
        userId: session.metadata!.userId,
        stripeSubscriptionId: session.subscription as string,
      })
      break
    }

    case 'customer.subscription.deleted': {
      const subscription = stripeEvent.data.object
      await serverConvexMutation(event, api.subscriptions.cancel, {
        stripeSubscriptionId: subscription.id,
      })
      break
    }
  }

  return { received: true }
})
```

::callout{icon="i-lucide-info"}
This pattern is appropriate only when the Convex mutation should stay inside your normal
user/business authorization model. If the webhook should act as a backend-only system task, switch
to the private bridge recipe instead of treating `serverConvex*` as a privileged lane.
::

### Authenticated API Route

```ts [server/api/user/profile.get.ts]
import { api } from '~~/convex/_generated/api'

export default defineEventHandler(async (event) => {
  const config = useRuntimeConfig()

  // Get auth token from session cookie
  const sessionCookie = getCookie(event, 'better-auth.session_token')
  if (!sessionCookie) {
    throw createError({ statusCode: 401, message: 'Unauthorized' })
  }

  // Exchange session for JWT token
  const tokenResponse = await $fetch<{ token?: string }>(
    `${config.public.convex.siteUrl}/api/auth/convex/token`,
    { headers: { Cookie: `better-auth.session_token=${sessionCookie}` } },
  )

  if (!tokenResponse?.token) {
    throw createError({ statusCode: 401, message: 'Invalid session' })
  }

  // Fetch with auth
  const profile = await serverConvexQuery(
    event,
    api.users.getProfile,
    {},
    { authToken: tokenResponse.token },
  )

  return profile
})
```

### Background Job Trigger (Privileged Lane)

```ts [server/api/cron/daily-digest.post.ts]
import { api } from '~~/convex/_generated/api'
import { privateConvexAction } from '../utils/private-convex'

export default defineEventHandler(async (event) => {
  // Verify cron secret
  const cronSecret = getHeader(event, 'x-cron-secret')
  if (cronSecret !== process.env.CRON_SECRET) {
    throw createError({ statusCode: 401 })
  }

  // Trigger a backend-only system action
  const result = await privateConvexAction(api.notifications.sendDailyDigests, {})

  return { sent: result.count }
})
```

For the full app-local bridge pattern, see
[System Workloads: Private Bridge](/docs/recipes/system-workloads-private-bridge).

That bridge should stay app-local infrastructure. Do not move it into a shared monorepo package or
into `better-convex-nuxt` itself.

### Server Middleware

```ts [server/middleware/analytics.ts]
import { api } from '~~/convex/_generated/api'

export default defineEventHandler(async (event) => {
  // Only track page views
  if (!event.path.startsWith('/api/')) {
    // Fire and forget - don't await
    serverConvexMutation(event, api.analytics.trackPageView, {
      path: event.path,
      userAgent: getHeader(event, 'user-agent') ?? '',
      timestamp: Date.now(),
    }).catch(() => {
      // Ignore errors - analytics shouldn't break the request
    })
  }
})
```

### External API Integration

```ts [server/api/import/github.post.ts]
import { api } from '~~/convex/_generated/api'

export default defineEventHandler(async (event) => {
  const { repoUrl } = await readBody(event)

  // Fetch from external API
  const [owner, repo] = repoUrl.replace('https://github.com/', '').split('/')
  const issues = await $fetch(`https://api.github.com/repos/${owner}/${repo}/issues`, {
    headers: { Authorization: `Bearer ${process.env.GITHUB_TOKEN}` },
  })

  // Store in Convex
  const imported = await serverConvexMutation(event, api.issues.importFromGithub, {
    repoUrl,
    issues: issues.map((i: any) => ({
      title: i.title,
      body: i.body,
      githubId: i.id,
    })),
  })

  return { imported: imported.count }
})
```

---

## Error Handling

```ts
export default defineEventHandler(async (event) => {
  try {
    const result = await serverConvexMutation(event, api.tasks.create, { text: 'New task' })
    return result
  } catch (error) {
    // Convex errors have a message property
    if (error instanceof Error) {
      throw createError({
        statusCode: 400,
        message: error.message,
      })
    }
    throw error
  }
})
```

---

## Debugging

Enable logging in your `nuxt.config.ts` to debug server-side operations:

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  convex: {
    url: process.env.CONVEX_URL,
    logging: {
      enabled: true, // Enable canonical log events
    },
  },
})
```

Server-side operations emit `operation:complete` events:

```
[better-convex-nuxt] ▷ query ✓ api.posts.list 45ms
[better-convex-nuxt] ▷ mutation ✓ api.tasks.create 67ms
```

See [Logging](/docs/advanced/logging) for full configuration options.

---

## TypeScript

Full type inference:

```ts
// Return type is inferred from query
const posts = await serverConvexQuery(event, api.posts.list, { status: 'published' })
// posts: Post[]

// Args are typed
await serverConvexMutation(
  event,
  api.posts.create,
  { title: 123 }, // Type error: title should be string
)
```

---

## Related Topics

- [Fetching Data](/docs/data-fetching/queries) - Client-side queries
- [Mutations](/docs/mutations/mutations) - Client-side mutations
- [Actions](/docs/mutations/actions) - Client-side actions
