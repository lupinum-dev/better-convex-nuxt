---
title: 'Functions'
description: 'Reference index for Trellis backend builder surfaces and runtime context.'
links:
  - label: Operations
    icon: i-lucide-workflow
    to: /docs/permissions/operations
  - label: API Surface
    icon: i-lucide-book-open
    to: /docs/api-reference/api-surface
---

Use this page when you need the main backend builder and function surfaces.

## Core Builders

- `defineTrellis`
- `query`
- `mutation`
- `action`
- `raw.query`
- `raw.mutation`
- `raw.action`

## Structured Primitives

- `defineOperation`
- `previewOf`
- `definePrincipal`
- `createComponentBridge`
- `defineComponentBridgeManifest`

## Runtime Context

Important context surfaces:

- `ctx.principal()`
- `ctx.actor()`
- `ctx.observe(...)`
- `ctx.db`
- `ctx.db.crossTenant`
- `ctx.db.raw`

## Notes

- `ctx.db` is the default safe path.
- `ctx.db.crossTenant` is the explicit tenant-boundary bypass while preserving more runtime guarantees.
- `ctx.db.raw` is the full escape hatch.
