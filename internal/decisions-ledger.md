# Better Convex vNext decision ledger

This is the canonical index of implementation decisions made while proving
[`RFC-better-convex-vnext.md`](./RFC-better-convex-vnext.md). The RFC owns normative product invariants;
this ledger records evidence-backed choices, rejected paths, and decisions that remain gated. The task
ledger points here instead of maintaining a second architectural history.

## Accepted decisions

| ID      | Decision                                                                                                                                                                                                                                     | Evidence                                                                                                                             | Consequence / deletion obligation                                                                                                                                                                                         |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `D-001` | MCP is optional and official-SDK-owned at the wire boundary; application Convex functions remain canonical.                                                                                                                                  | RFC product boundary; `internal/evidence/mcp-spec-checkpoint-2026-07-20.md`                                                          | No proprietary agent protocol, automatic function dispatcher, or application-policy database.                                                                                                                             |
| `D-002` | `2025-11-25` remains the latest final MCP revision at the 2026-07-20 checkpoint; `2026-07-28` behavior is private RC evidence only until publication.                                                                                        | `internal/evidence/mcp-spec-checkpoint-2026-07-20.md`                                                                                | Public MCP API stabilization remains gated by `P1-015`.                                                                                                                                                                   |
| `D-003` | The neutral topology workload is application-owned notes state with explicit search, rename, delete, report, and resource operations.                                                                                                        | `internal/labs/mcp-topology/neutral/notes-application.ts`; `test/unit/vnext-neutral-notes.test.ts`                                   | No transport abstraction, approval framework, or job state in the neutral domain.                                                                                                                                         |
| `D-004` | Exact MCP SDK v2 beta packages are dev-only private laboratory dependencies.                                                                                                                                                                 | `internal/evidence/mcp-sdk-lab-pin-2026-07-20.md`                                                                                    | Replace/reconcile the pin after the final SDK; no final support claim.                                                                                                                                                    |
| `D-005` | The official MCP server SDK is executable in a deployed Convex HTTP action without a parser fork, Node polyfill, or patch.                                                                                                                   | `internal/evidence/mcp-convex-probe-2026-07-20.md`; `internal/evidence/mcp-runtime-purity-2026-07-20.md`                             | Convex-native remains preferred while later hard gates are proved.                                                                                                                                                        |
| `D-006` | Nitro exact-call fallback is technically viable with Ed25519, pinned Convex canonical encoding, fixed wrappers, and application-owned replay. It remains private.                                                                            | `internal/evidence/mcp-nitro-exact-call-2026-07-21.md`                                                                               | If Nitro loses, delete the whole prototype. If it wins, retain no generic dispatcher or public Trusted Calls product.                                                                                                     |
| `D-007` | Provider-neutral local Convex fixtures may require function readiness without fabricated Better Auth routes or injected auth secrets.                                                                                                        | Deployed `P1-011` proof; `test/helpers/local-convex.ts`                                                                              | Better Auth readiness remains the existing default; neutral mode is explicit and test-only.                                                                                                                               |
| `D-008` | Candidate A has one bearer-consuming `/mcp` action; application operations receive only the projected external subject and remain internal.                                                                                                  | `internal/evidence/mcp-convex-bearer-boundary-2026-07-21.md`                                                                         | No bearer bridge, general token argument, alternate verifier route, or fake auth readiness endpoint.                                                                                                                      |
| `D-009` | Published `2025-11-25` URL elicitation is not a product target for the candidates' stateless HTTP mode because the tool request cannot observe initialize capabilities.                                                                      | `internal/evidence/mcp-url-interaction-gate-2026-07-21.md`                                                                           | Keep direct interaction gated on the final stateless protocol; add no session store, custom capability channel, or clickable fallback.                                                                                    |
| `D-010` | One credential-free Vue MCP App can use official `App`/`AppBridge` against both candidates; server registration stays on each candidate's v2 SDK until Apps ships a compatible helper.                                                       | `internal/evidence/mcp-apps-probe-2026-07-21.md`                                                                                     | Phase 7 must clone Vue reactive values, re-project app tool calls, prove the official web sandbox, and add no v1/v2 cast or second wire server.                                                                           |
| `D-011` | One reactive `'skip'` sentinel is the canonical Vue/Nuxt query execution gate; no overlapping `enabled` option is admitted.                                                                                                                  | `internal/evidence/canonical-query-gate-2026-07-22.md`                                                                               | Preserve the smaller existing Nuxt contract; entering skip must unsubscribe and synchronously clear protected/current and previous data.                                                                                  |
| `D-012` | Build `better-convex-vue` from the Nuxt-used source island; do not wrap, fork, or copy the community `convex-vue` client.                                                                                                                    | `internal/decisions/ADR-vnext-vue-boundary.md`; exact `convex-vue@0.1.5` behavioral probe                                            | Collaborate upstream on invariants/API shape, but retain one proven engine until an official/community implementation passes the same matrix.                                                                             |
| `D-013` | Admit one minimal Vue plugin/composable surface plus an isolated embedded attachment; keep controllers private. `D-014` later admitted the proved provider auth types.                                                                       | `internal/decisions/ADR-vnext-vue-public-api.md`; Phase 3 Nuxt/Vite/embedded and packed evidence                                     | Move the source once and export no lifecycle controls, provider clients, token refs, roles, permissions, or raw Convex clients.                                                                                           |
| `D-014` | Use the proved provider-neutral token/state seam before the atomic Vue package move and Nuxt hard cut.                                                                                                                                       | `internal/decisions/ADR-vnext-vue-auth-seam.md`; `internal/evidence/vue-auth-seam-proof-2026-07-22.md`                               | Vue owns raw clients, `setAuth`, identity retirement, and disposal; adapters expose no client controls, secrets, roles, or provider user model.                                                                           |
| `D-015` | Admit only token-free host-adapter controls (`attachment`, settlement, auth reconfirmation) plus a complete SSR first-page seed.                                                                                                             | Atomic Nuxt cutover, embedded host, and identity-partitioned query/pagination hydration tests                                        | Nuxt uses public Vue entries without raw client controls; `initialPage` remains an adapter primitive, not page metadata or a second cache.                                                                                |
| `D-016` | Expose the existing frozen Vue attachment to Nuxt host applications through one narrow composable; do not expose the internal Nuxt runtime context or add another bridge model.                                                              | `internal/decisions/ADR-nuxt-embedded-attachment.md`; focused Nuxt proof; clean Ginko baseline                                       | Embedded hosts pass only the token-free attachment; Ginko later deletes its client/auth-ref lifecycle bridge rather than adapting it again.                                                                               |
| `D-017` | Use existing attachment/composables for Ginko; add no BCN preflight, page-metadata, profile, or upload API.                                                                                                                                  | `internal/evidence/ginko-vue-hard-cut-map-2026-07-22.md`; clean Ginko baseline and 30 tests                                          | Ginko wraps one attachment for contract checks, splits the facets query, retains upload/policy, and deletes its lifecycle engines.                                                                                        |
| `D-018` | Advance the private MCP laboratory from SDK beta.4 to beta.5 only after exact-byte review and deployed modern/legacy proof in both candidates.                                                                                               | `internal/evidence/mcp-sdk-beta5-reconciliation-2026-07-22.md`                                                                       | Treat the changed result metadata as RC evidence only; `P1-015`, URL interaction, topology selection, and every public MCP claim remain gated.                                                                            |
| `D-019` | Use Convex-native as the vNext MCP implementation direction now; freeze Nitro as a restoration fallback rather than maintaining peer feature parity.                                                                                         | Completed dual-topology laboratory, beta.5 production probes, owner approval on 2026-07-22                                           | Build only the Convex-native package path; preserve Nitro on a frozen branch/capsule and delete it from the active tree after replacement proof.                                                                          |
| `D-020` | Begin `@better-convex/mcp` as an experimental prerelease against exact locked-RC SDK bytes while retaining final-spec reconciliation as a stabilization gate.                                                                                | Locked RC publication model, exact beta.5 reconciliation, owner approval on 2026-07-22                                               | Public API admission may proceed; no final-compliance claim or RC compatibility shim, and final bytes may require a hard cut before publication.                                                                          |
| `D-021` | Use Better Convex as the product family and prerelease-first delivery strategy without adding a catch-all package or renaming the repository before package cutover.                                                                         | RFC product model and owner approval on 2026-07-22                                                                                   | Keep current package names, coordinate experimental candidates, and retain `G-005` for the consequential repository rename.                                                                                               |
| `D-022` | Admit one Convex MCP hosting adapter and safe access-context/verifier seam; keep official SDK registration, schemas, protocol, OAuth projection, and errors direct.                                                                          | `internal/decisions/ADR-mcp-public-api-admission.md`; neutral and Ginko boundary comparison                                          | Add no second tool registry, scope helper, result framework, authorization DSL, or raw `AuthInfo` application surface.                                                                                                    |
| `D-023` | Place the optional Better Auth MCP verifier factory in the existing `better-convex-nuxt/convex-auth` backend entry and rely on structural compatibility with the provider-neutral seam.                                                      | `internal/decisions/ADR-mcp-better-auth-adapter-placement.md`; exact MCP dependency and consumer proof                               | Reuse the hardened verifier; add no MCP→Better Auth dependency, provider subpath in MCP, fourth adapter package, or copied JWT verifier.                                                                                  |
| `D-024` | Admit an explicit preconfigured-bearer MCP authorization profile for application-managed credentials without publishing fictional OAuth metadata.                                                                                            | Official client externally managed token support; Ginko credential model; `internal/evidence/mcp-preconfigured-bearer-2026-07-22.md` | Keep OAuth and preconfigured profiles disjoint; no token provisioning/store, discovery claim, provider policy, or raw bearer application surface.                                                                         |
| `D-025` | Admit one optional Vue MCP App lifecycle composable around a private official `App`; expose only cloned `callServerTool`/`openLink`, force automatic resize off, and add no plugin, server-major adapter, host SDK, token, or Convex client. | `internal/decisions/ADR-vue-mcp-app-api-admission.md`; `internal/evidence/vnext-vue-mcp-app-boundary-stabilization-2026-07-23.md`    | The subpath stays experimental until upstream logger control plus exact neutral/Ginko, different-origin, and real-host evidence pass; registration remains direct official v2 while the Apps helper is v1-peer-coupled.   |
| `D-026` | Freeze feature expansion and retire the unpublished beta.6/beta.0 source identities until the audit stabilization re-entry gate passes.                                                                                                      | `Audit.md`; owner-approved stabilization plan; `S0-001`                                                                              | Correct lifecycle, SSR/auth storage, MCP and Ginko boundaries first; reserve beta.7/beta.1 only after every P0 fix and candidate gate passes.                                                                             |
| `D-027` | The Convex-native MCP adapter serves only strict modern, finite JSON tools and resources; legacy serving, subscriptions, list-change claims, and streaming responses are rejected.                                                           | Audit `F-006`–`F-008`; `internal/evidence/vnext-mcp-unary-boundary-stabilization-2026-07-23.md`                                      | Preserve official SDK protocol ownership without adding a session bus, SSE exception, distributed subscription registry, or second topology.                                                                              |
| `D-028` | The Better Auth MCP verifier requires a request-local live authority callback after strict token verification; offline external verifiers retain expiry-bounded provider revocation semantics.                                               | Audit `F-009`; `internal/evidence/vnext-mcp-better-auth-live-validation-2026-07-23.md`                                               | Provider-private session identity stays inside the adapter callback and never enters the provider-neutral access context.                                                                                                 |
| `D-029` | Publish the closed Vue→Nuxt set and separate MCP lane only under `next-staging`; stop automation before shared dist-tag promotion.                                                                                                           | npm trusted-publishing/staged-publishing command limits; `internal/evidence/vnext-closed-release-lanes-stabilization-2026-07-23.md`  | Preserve OIDC-only publication and exact registry-byte gates; shared user-facing tags require a later interactive maintainer action rather than a long-lived automation token.                                            |
| `D-030` | Ginko untrusted CI validates its own source only; upstream compatibility authority comes exclusively from immutable artifact manifests and compatibility-pinned bytes.                                                                       | Ginko `80a326a4`; `internal/evidence/ginko-ci-artifact-authority-stabilization-2026-07-23.md`                                        | Delete private-read credentials and upstream source repacking; add MCP only with its fresh exact candidate rather than retaining the Git override as a second lane.                                                       |
| `D-031` | The locally generated Vue/Nuxt beta.7 and beta.8 sets are failed immutable candidates and are retired; use beta.9 without deleting or rebuilding either set.                                                                                 | `Audit.md`; beta.7 ASVS failure; beta.8 provenance failure; corrected gates                                                          | Preserve immutable-coordinate semantics even for unpublished local candidates; beta.9 is the first eligible successor Vue/Nuxt set, while untouched MCP beta.1 remains eligible.                                          |
| `D-032` | Do not relay initialize-era conformance traffic into the RC-only MCP server; certify the locked RC with the exact official v2 client until matching official scenarios ship.                                                                 | `@modelcontextprotocol/conformance@0.1.16` rejects `2026-07-28`; `internal/evidence/mcp-rc-conformance-2026-07-22.md`                | Delete the legacy relay and its tests; retain `P9-004` as the final-spec/tool reconciliation gate rather than weakening the truthful stateless protocol surface.                                                          |
| `D-033` | Retire the failed Vue/Nuxt beta.9 candidate and use beta.10 after correcting the packed lifecycle fixture's incomplete `convex/browser` surface.                                                                                             | beta.9 exact-package production lifecycle build failure; corrected beta.9 tarball regression proof                                   | Never rebuild beta.9; require the corrected packed production consumer to pass as part of the beta.10 immutable candidate set.                                                                                            |
| `D-034` | Retire MCP beta.1 after its exact external Convex consumer exposed legacy initialize negotiation; supersede the passing Vue/Nuxt beta.10 pair with beta.11 so all three packages share one clean source commit.                              | MCP beta.1 external-consumer failure; locked-RC stateless discovery proof; beta.10 exact candidate set passed                        | Never rebuild beta.1 or beta.10; certify MCP beta.2 and Vue/Nuxt beta.11 only after preliminary exact-tarball consumers pass the RC `server/discover` envelope.                                                           |
| `D-035` | Retire Vue/Nuxt beta.11 and MCP beta.2 after post-candidate review exposed an SSR status defect and duplicate-SDK MCP composition boundary; reserve Vue/Nuxt beta.12 and MCP beta.3.                                                         | `Audit.md` §18; `internal/evidence/vnext-thermo-review-corrections-2026-07-23.md`                                                    | Never rebuild the superseded coordinates; exact Ginko and R0 evidence must use fresh artifacts built from the corrected clean source commit.                                                                              |
| `D-036` | Retire MCP beta.3 after its exact external consumer retained the removed consumer-owned SDK lifecycle; reserve MCP beta.4 after correcting the proof itself.                                                                                 | Failed beta.3 `check-mcp-package-consumer`; corrected proof passes against the immutable beta.3 bytes                                | Never rebuild beta.3; candidate certification must prove the public lifecycle through installed bytes, not only source callers and type-only imports.                                                                     |
| `D-037` | Retire MCP beta.4 after release-only MCP documentation evidence retained wording from the removed lifecycle; reserve MCP beta.5 only after the complete MCP project passes.                                                                  | beta.4 release verification; corrected `pnpm exec vitest run --project=mcp` (8 files, 55 tests)                                      | Never rebuild beta.4; release-only suites are candidate authority and must be run before committing the next coordinate.                                                                                                  |
| `D-038` | Retire Vue/Nuxt beta.12 after its immutable set reached the newly published High PostCSS advisory; reserve beta.13 with patched PostCSS while retaining independently certified MCP beta.5.                                                  | `GHSA-6g55-p6wh-862q`; beta.12 `check:auth-advisories`; MCP beta.5 production SBOM                                                   | Never rebuild beta.12 or suppress the advisory; the independent MCP lane need not be rebuilt for a build-only dependency absent from its artifact.                                                                        |
| `D-039` | Retire Vue/Nuxt beta.13 after its immutable set reached stale auth-derived provenance records; reserve beta.14 after reconciling all 28 source and packed mappings while retaining independently certified MCP beta.5.                       | beta.13 `verify:auth`; `security/upstream-convex-better-auth.json`; passing source and packed provenance check                       | Never rebuild beta.13 or weaken provenance coverage; extracted derived files must receive explicit records and changed targets must update their hashes before a new coordinate is reserved.                              |
| `D-040` | Retire Vue/Nuxt beta.14 after the OAuth/MCP starter attempted to resolve retained MCP beta.5 from the public registry; reserve beta.15 only after the matrix installs and byte-verifies the immutable retained MCP artifact.                 | beta.14 candidate app failure; passing exact beta.14 Vue/Nuxt plus MCP beta.5 consumer matrix                                        | Never rebuild beta.14 or let unpublished companion dependencies fall through to a registry; every package used by a maintained candidate app is supplied from reviewed immutable evidence.                                |
| `D-041` | Pass the local stabilization R0 gate and resume Phase 7 experimental work without treating the candidate as publishable or stable.                                                                                                           | `internal/evidence/vnext-r0-security-rereview-2026-07-23.md`; exact beta.15/beta.5 and Ginko candidate evidence                      | The stabilization feature freeze ends locally; protected staging, final MCP/host reconciliation, registry equality, publication authority, and stable support claims remain gated.                                        |
| `D-042` | Keep the application-owned high-impact operation core independent of MCP vocabulary and isolate the locked-RC URL-elicitation mapping inside the private Convex-native laboratory until final-spec reconciliation.                           | `cfc2a020`; `internal/evidence/mcp-high-impact-interaction-rc-proof-2026-07-24.md`                                                   | Application state, current authority, current impact, effect, and receipt remain canonical; host acceptance and navigation grant no authority; no RC type or helper is publicly exported before `P1-015`.                 |
| `D-043` | Prove direct same-subject interaction as an inert application page plus explicit fixed-origin POST; keep login/session mapping and page composition application-owned rather than adding a Nuxt or MCP approval abstraction.                 | `9d6d0d7f`; `internal/evidence/mcp-high-impact-interaction-production-proof-2026-07-24.md`                                           | The shared product may later project final protocol vocabulary, but it owns no page, session, review table, policy, or effect; Ginko must project its existing canonical records instead of adopting the neutral fixture. |

## Pending decision gates

| ID      | Decision required                                                                                                                            | Entry evidence                                                            | Current status                                                                                                                                    |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `G-001` | Certify or explicitly reopen the selected Convex-native topology after final spec/SDK, conformance, hosts, latency and operational evidence. | `P1-015`–`P1-017`, Convex package proof, frozen Nitro restoration capsule | Implementation direction accepted in `D-019`; final support remains open and must fail back explicitly rather than creating peer implementations. |
| `G-004` | Decide whether Tasks is admitted at all.                                                                                                     | Every `P8-001` entry condition                                            | Blocked; Tasks do not block 1.0.                                                                                                                  |
| `G-005` | Rename/rebrand the repository after package cutover.                                                                                         | Phase 4 exact-package cutover and owner authority                         | Deferred; no early symmetry move.                                                                                                                 |

## Rejected product paths

| ID      | Rejected path                                                            | Reason / simpler retained path                                                                                 |
| ------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------- |
| `R-001` | Public `@better-convex/core`                                             | Keep one private client source island until another framework proves a public contract is necessary.           |
| `R-002` | Public Trusted Calls or generic service-proof package                    | Exact-call proof is a topology-specific internal fallback. Multiple non-MCP consumers would require a new RFC. |
| `R-003` | Commands/prepare-confirm package                                         | Ordinary writes remain tools; high-impact work uses negotiated MCP interaction over application-owned state.   |
| `R-004` | Universal principal/RBAC/authorization DSL                               | Preserve identity provenance; applications map `(issuer, subject)` and re-read current authority.              |
| `R-005` | Automatic exposure or generic dispatch of Convex functions               | Every tool/resource maps explicitly to one application operation.                                              |
| `R-006` | Two maintained MCP protocol topologies                                   | Complete both probes, select one, delete the loser.                                                            |
| `R-007` | Better-Convex-owned approval, handoff, workflow, replay, or job database | Project application-owned canonical records and idempotency.                                                   |
| `R-008` | Legacy Tasks API or speculative Tasks compatibility                      | Wait for final extension, SDK, two clients, and a real deferred job that cannot use structured status.         |
| `R-009` | Better Auth as the provider-neutral MCP boundary                         | Keep it as an optional first-party adapter and prove an external verifier.                                     |
| `R-010` | Roles/permissions in tokens or OAuth scopes as current authority         | Scopes are ceilings; each effect reloads canonical application authority.                                      |

## Update rule

- Add or amend an entry in the same commit as the evidence that changes the decision.
- Never silently rewrite an accepted decision. Record replacement evidence and the superseded ID.
- A pending gate is not an accepted API.
- Detailed experiments belong in `internal/evidence/**`; this file retains only the durable conclusion.
