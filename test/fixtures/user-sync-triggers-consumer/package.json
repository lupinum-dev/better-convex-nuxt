{
  "name": "user-sync-triggers-consumer",
  "private": true,
  "description": "Packed-probe consumer for better-convex-nuxt/server/createUserSyncTriggers (internal §16.2). Imports ONLY this subpath — no Nuxt, no Vue, no Convex server runtime — and exercises it from a real node_modules-resident install of the packed tarball.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "better-convex-nuxt": "file:./better-convex-nuxt.tgz",
    "typescript": "5.9.3"
  }
}
