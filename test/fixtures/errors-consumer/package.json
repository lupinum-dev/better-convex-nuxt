{
  "name": "errors-consumer",
  "private": true,
  "description": "Packed-probe consumer for better-convex-nuxt/errors (vNext §7 new-files list; internal §16.2). Imports ONLY the /errors subpath.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "better-convex-nuxt": "file:./better-convex-nuxt.tgz",
    "typescript": "5.9.3"
  }
}
