// The "typed empty fallback": a built-in empty definition with no additional
// plugins (vNext §8 default resolution step 3).
import { defineConvexAuthClient } from 'better-convex-nuxt/auth-client'

export default defineConvexAuthClient()
