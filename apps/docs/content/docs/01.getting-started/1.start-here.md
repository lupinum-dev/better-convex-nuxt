---
title: 'Start here'
description: "What Trellis is, what it isn't, and how to start with the smallest real app shape."
links:
  - label: Installation
    icon: i-lucide-download
    to: /docs/getting-started/installation
  - label: App layout
    icon: i-lucide-folder-tree
    to: /docs/getting-started/canonical-app-layout
  - label: Examples
    icon: i-lucide-layout-template
    to: https://github.com/lupinum-dev/trellis/tree/main/examples
    target: _blank
---

Trellis is the opinionated path for repeated apps on the same stack:

- Nuxt
- Convex
- Better Auth
- MCP

It is not a generic integration helper.

## Fastest on-ramp

Create a real app root first:

```bash
npx trellis init my-app --template personal
```

Or start directly on the workspace lane:

```bash
npx trellis init my-app --template workspace
```

If the workspace app also needs MCP from day one:

```bash
npx trellis init my-app --template workspace --mcp
```

Use `cms` when the product is a public content surface plus a signed-in editorial studio.

## Pick the smallest real lane

| Starting point      | Pick it when                                            | Read first                                                                                                 |
| ------------------- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| **Public-only app** | You want SSR and live queries with no sign-in flow yet. | [`01-public-todo`](https://github.com/lupinum-dev/trellis/tree/main/examples/01-public-todo)               |
| **Personal app**    | You need signed-in users but not tenant-scoped data.    | [`02-auth-todo`](https://github.com/lupinum-dev/trellis/tree/main/examples/02-auth-todo)                   |
| **Workspace app**   | You need roles, tenant data, or trusted server callers. | [`03-team-workspace`](https://github.com/lupinum-dev/trellis/tree/main/examples/03-team-workspace)         |
| **CMS app**         | You need public reads plus a signed-in studio.          | [`08-component-mini-cms`](https://github.com/lupinum-dev/trellis/tree/main/examples/08-component-mini-cms) |

::tip
If you are unsure, choose the smaller lane. Trellis should let simple apps stay simple.
::

## Day-one mental model

You only need three ideas up front:

1. Browser and Nitro edge schemas live in `shared/schemas/`.
2. Backend features and protected input contracts live in `convex/domain/`.
3. Auth, permissions, operations, and MCP are added only when the app crosses that boundary.

Everything else is a deeper layer, not day-one homework.

## What success looks like

The first visible win is still small:

1. install the module
2. point it at Convex
3. render one query
4. see the page update without writing refetch glue

From there, Trellis grows into auth, permissions, operations, and MCP without changing the app’s core shape.

## Add layers only when needed

- **Auth** when the app needs signed-in callers
- **Permissions** when the browser needs backend-owned capability projection
- **Operations** when destructive work needs preview/confirm
- **MCP** when the same backend must serve an agent client

## Next

Read [Installation](/docs/getting-started/installation), then [Canonical app layout](/docs/getting-started/canonical-app-layout), then pick either [First live query](/docs/getting-started/first-live-query) or [Build a signed-in todo app](/docs/getting-started/build-a-signed-in-todo-app).
